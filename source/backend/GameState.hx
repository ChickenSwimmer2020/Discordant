package backend;

class GameState {
    //so, something I learned is that its apparently possible to have a varible in one state that never changes no matter what unless specifically targetted.
    //kinda like a savestate in an old console rerun, maybe we can use something like that for holding the current player stats and stuff
}